<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:defaultValue="0.5"
        android:entries="@array/pref_increments"
        android:entryValues="@array/pref_increments_values"
        android:key="@string/increments_list_key"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/increment_by" />
    <EditTextPreference
        android:defaultValue="6.5"
        android:inputType="numberDecimal"
        android:key="@string/target_editText_key"
        android:title="Target" />

</PreferenceScreen>